// import { connect } from 'react-redux'

// function AcademicProcessesDetail({ academicProcesses }) {
//   return (
//     <div>
//       {academicProcesses.map((academicProcess, index) => (
//         <h2 key={index}>{academicProcess.id}</h2>
//       ))}
//     </div>
//   )
// }

// const mapStateToProps = (reducers) => {
//   return reducers.usersReducers
// }

// export default connect(mapStateToProps)(AcademicProcessesDetail)
